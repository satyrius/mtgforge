<div class='serp-row'>
    <% for (i in cards) { %>
        <% var card = cards[i] %>
        <span class="card">
            <img src="<%= card.thumb %>" class="color_identity_<%= card.color_identity %>" data-id="<%= card.id %>"/>
        </span>
    <% } %>
</div>
